<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.comic.mapper.ReadHistoryMapper">
    <insert id="insert" parameterType="com.comic.entity.ReadHistory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_read_history (user_id, comic_id, chapter_id)
        VALUES (#{userId}, #{comicId}, #{chapterId})
    </insert>

    <select id="selectByUserId" parameterType="Long" resultType="com.comic.entity.ReadHistory">
        SELECT * FROM t_read_history
        WHERE user_id = #{userId} ORDER BY read_time DESC
    </select>
</mapper>